# Global Math Fabric

Monorepo:
- server/ : platform API + scheduler
- client/ : user-side agent (polls tasks, runs sandboxed jobs)
- infra/  : docker-compose and local dev infrastructure

Local dev:
  docker compose up --build
